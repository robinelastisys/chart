name: Push package to GHCR

env:
  DOTNET_VERSION: '6.0.x'

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
      push-store-image:
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: './chart'
        steps:
          - name: 'Checkout GitHub Action'
            uses: actions/checkout@main

          - name: 'Package and push'
            run: |
              helm package .
              helm registry login ghcr.io -u robinelastisys --password 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
              export CHART_NAME=$(grep 'name:' Chart.yaml | tail -n1 | awk '{ print $2 }') 
              export CHART_VERSION=$(grep 'version:' Chart.yaml | tail -n1 | awk '{ print $2 }')
              helm push ${CHART_NAME}-${CHART_VERSION}.tgz oci://ghcr.io/robinelastisys